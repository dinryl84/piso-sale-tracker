name: Check Piso Sale

on:
  schedule:
    # Runs once a day at 01:00 UTC (09:00 Philippine time)
    - cron: "0 1 * * *"
  workflow_dispatch: {}  # lets you trigger it manually from the Actions tab

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run checker
        env:
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
        run: python check_promo.py

      - name: Commit updated state
        run: |
          git config user.name "piso-sale-bot"
          git config user.email "actions@users.noreply.github.com"
          git add seen.json
          git diff --quiet && git diff --staged --quiet || git commit -m "Update seen.json [skip ci]"
          git push
